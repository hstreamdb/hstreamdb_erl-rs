{erl_opts, [debug_info]}.

{deps, []}.

{artifacts, ["priv/libhstreamdb_erl_nifs.so"]}.

{project_plugins, [
    erlfmt
]}.

{erlfmt, [write]}.

{pre_hooks, [
    {
        compile,
        "$(cd rs_src/hstreamdb-rust/ && cargo build --release && mkdir -p ../../priv && cp target/release/libhstreamdb_erl_nifs.so ../../priv)"
    }
]}.
